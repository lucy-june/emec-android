adapter包是适配器类包，包含页面（UI包）的数据源处理。
bean包实体类包，包含各种实体类，例如商品。
config应该是配置用的，现在还用不到。
entity包目前还不知道干什么的。
image包是图片加载的一些类。（将要被替换到common包内）。
task不知道干什么的。
UI包包含了每个页面。
base包是页面的一个父类。
utils是一些基础的工具类包。
widgets是一些自定义的控件
jazzviewpager是首页里面的一些控件。
viewpager也是首页的东西
zxing是二维码扫描的一些包，现在可以不用管。